with Tableau_Entiers_Tries ; use Tableau_Entiers_Tries ;

procedure main is
   tab : Tab_Entiers_Tries ;
   n : Integer ; -- nombre d elements du tableau

begin
   -- rentre les valeurs triees dans le tableau
   lire (tab, n) ;

   -- affiche les elements du tableau
   ecrire (tab, n) ;

end main ;